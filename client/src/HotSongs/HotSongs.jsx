import React from 'react';
// import useFetchData from '../customHooks/useFetchData.jsx';
import WillBeReadySoon from '../WillBeReadySoon.tsx';

function HotSongs() {
  // const { data } = useFetchData('http://localhost:3001/songs');
  // if (data) console.log(data);
  return (
    <>
      <WillBeReadySoon />
    </>
  );
}

export default HotSongs;
