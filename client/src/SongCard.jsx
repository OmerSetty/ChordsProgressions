import React from 'react';

function SongCard({ song }) {
  // function displayProgressionsByParts(part) {
  //   const abc = part.progressions.map(progression => {
  //     return song.progressionsData.find(prog => progression.progression === prog._id).progression.join(' ');
  //   });
  //   return abc;
  // }

  return (
    <>

    </>
  );
}

export default SongCard;